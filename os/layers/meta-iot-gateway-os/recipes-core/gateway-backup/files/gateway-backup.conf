# gateway-backup configuration
# Loaded by gateway-backup.service via EnvironmentFile=

# Remote rsync/ssh destination, e.g. "backup@backup-host.example.com:/srv/gateway-backups/"
GATEWAY_BACKUP_REMOTE=""
