interface=mlan0
driver=nl80211

# Open network - no password needed; portal itself handles auth
ssid=Gateway-Setup
hw_mode=g
channel=6
auth_algs=1
ignore_broadcast_ssid=0

# Country code - required for regulatory compliance
country_code=DE
ieee80211d=1
