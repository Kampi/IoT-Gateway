# otbr-agent configuration for IoT Gateway
# Loaded by otbr-agent.service via EnvironmentFile=

# RCP device (e.g. nRF52840 flashed with RCP firmware, connected via USB)
OTBR_AGENT_OPTS="-I wpan0 -B @OTBR_INFRA_IFACE@ spinel+hdlc+uart://@OTBR_RCP_DEVICE@ trel://@OTBR_INFRA_IFACE@"
